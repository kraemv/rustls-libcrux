[workspace]
members = ["agent-setup", "demo-server", "key-importer", "libcrux-provider"]

resolver = "2"

[workspace.dependencies]
rustls-libcrux-provider = { path = "libcrux-provider" }

[profile.bench]
codegen-units = 1
lto = true
